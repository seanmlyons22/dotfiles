" Don't prompt when switching unsaved buffers
set hidden
" Enable loading plugins based on filetype
filetype plugin on
" Enable line numbers
set number
" Enable relative line numbers
set relativenumber
" Set tab stop and shift width to 4 spaces
set ts=4 sw=4
